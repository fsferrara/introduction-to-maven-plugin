/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package io.github.fsferrara.app;

import io.github.fsferrara.list.LinkedList;

import static io.github.fsferrara.utilities.StringUtils.join;
import static io.github.fsferrara.utilities.StringUtils.split;
import static io.github.fsferrara.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
