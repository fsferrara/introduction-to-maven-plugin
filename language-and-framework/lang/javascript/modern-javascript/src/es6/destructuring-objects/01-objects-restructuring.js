/*
Destructuring an object means to extract some properties out of it.
*/

// Picking only 3 properties from `Math`.
const {PI, E, SQRT2} = Math;

console.log(PI);
console.log(E);
console.log(SQRT2);
